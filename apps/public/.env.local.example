# Email Configuration
# For development, you can use Gmail SMTP or leave these empty to use defaults

# Gmail SMTP (recommended for development)
# 1. Go to Google Account settings > Security > 2-Step Verification
# 2. Generate an "App Password" for this application
# 3. Use your Gmail address and the app password below
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=your-16-character-app-password

# Production SMTP (for when you deploy)
SMTP_HOST=smtp.yourdomain.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=noreply@yourdomain.com
SMTP_PASS=your-smtp-password

# From email address
FROM_EMAIL="Scotch Doubles" <noreply@scotchdoubles.com>

# Next.js
NEXT_PUBLIC_APP_URL=http://localhost:3002